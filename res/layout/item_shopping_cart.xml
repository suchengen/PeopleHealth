<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="80dp"
    android:orientation="vertical" >

    <ImageView
        android:id="@+id/iv_shopping_cart_intro"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_centerVertical="true"
        android:layout_marginLeft="20dp"
        android:background="@android:color/black" />

    <TextView
        android:id="@+id/tv_shopping_cart_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/iv_shopping_cart_intro"
        android:layout_marginLeft="10dp"
        android:layout_toRightOf="@+id/iv_shopping_cart_intro"
        android:ellipsize="end"
        android:singleLine="true"
        android:text="fjdsaifjdsaofjdsaijo" />

</RelativeLayout>